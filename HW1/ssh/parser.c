#include "parser.h"

int parse(char* test)
{
    int len=strlen(test);
    int i;
    for(i=0; i<len; i++)
    {
        printf(" ");
    }
    return 0;
}

int numcheck(char* cNum)
{
    int i=0;
    while(char[i] != '\0')
    {

    }
    return 0;
}
